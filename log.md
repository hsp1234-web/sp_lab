**日期**: `2025-11-10 12:09:03 CST`
**作者**: Jules
**標題**: **v2.6 引入 `run.py` 根除匯入衝突並優化執行流程**

### 1. 摘要

本次更新 (v2.6) 旨在從根本上解決 Python 模組匯入路徑的衝突問題，並進一步簡化專案在本地與 Colab 環境中的執行方式。我們引入了一個位於專案根目錄的 `run.py` 腳本作為唯一的、受控的程式進入點。此腳本會強制將 `src` 目錄設為最高優先級的搜尋路徑，從而徹底解決了本地 `signal.py` 模組與 Python 系統內建函式庫的命名衝突問題。相應地，我們也更新了 `build_notebook.py` 來產生新的 `sp_lab.ipynb`，使其直接呼叫此 `run.py`，並將 Colab 徽章與 `README.md` 中的文件同步更新至 `2.6` 版本。

### 2. 重大決策與技術挑戰

#### 2.1. 根除 `ImportError`：引入 `run.py` 執行入口

-   **問題**: 在 Colab 環境中，`from signal import ...` 語句會錯誤地匯入 Python 系統內建的 `signal` 函式庫，而非我們專案的 `src/signal.py`，導致 `ImportError`。
-   **解決方案**: 我們不再採用修補 Colab 執行環境的方式，而是建立了一個更穩健的架構。在專案根目錄新增 `run.py` 腳本。
    1.  **動態路徑設定**: `run.py` 的核心職責是在執行任何專案程式碼前，先將 `src` 目錄的絕對路徑以 `sys.path.insert(0, ...)` 的方式，強制注入到 Python 搜尋路徑的**最前端**。
    2.  **單一進入點**: `run.py` 接著會匯入並執行 `src/main.py` 的主流程。所有執行（無論是本地端或 Colab）都透過 `python run.py` 這一個統一的指令完成。
-   **優勢**: 這個方法不僅解決了命名衝突，還讓 `src` 目錄下的所有模組可以繼續使用乾淨、無關於路徑操作的程式碼，提升了整體的模組化與可維護性。

#### 2.2. Colab Notebook 流程簡化

-   **挑戰**: 為了配合新的 `run.py` 架構，需要更新 `sp_lab.ipynb` 的執行邏輯，同時確保使用者體驗（如程式碼預設收摺）不變。
-   **解決方案**:
    1.  **修改 `build_notebook.py`**: 我們更新了這個筆記本生成腳本，使其產生的 `sp_lab.ipynb` 第三個儲存格的邏輯大幅簡化。移除了所有手動操作 `sys.path` 的程式碼，並將執行指令從 `%run src/main.py` 改為在專案根目錄下執行 `%run run.py`。
    2.  **新增依賴**: 在此過程中，發現執行 `build_notebook.py` 需要 `nbformat` 函式庫。我們已將此依賴明確地加入 `requirements.txt`，確保了開發環境的可重現性。

#### 2.3. 文件與版本同步

-   **版本號**: 由於本次架構優化影響了核心執行流程，我們將專案版本號提升至 `2.6`。
-   **文件更新**:
    -   `README.md` 中的 Colab 徽章連結已更新至 `2.6` 分支。
    -   `README.md` 的「快速開始」章節也已同步修改，明確指示使用者透過 `python run.py` 來執行專案。

### 3. 總結

v2.6 的更新是一次重要的架構重構。透過引入 `run.py` 作為專案的統一執行入口，我們不僅優雅地解決了模組命名衝突的問題，還使得專案的執行流程更加清晰、穩健。這為未來的擴充與維護，建立了一個更可靠的基礎。

---

**日期**: `2025-11-09 18:25:00 CST`
**作者**: Jules
**標題**: **v2.5 Colab Notebook 優化與匯入問題修正**

### 1. 摘要

本次更新專注於提升 Google Colab 使用者體驗以及修復在該環境下的模組匯入錯誤。我們透過程式化生成 Notebook 的方式，為所有儲存格加入了「預設收合」的元數據，使介面更簡潔。同時，徹底解決了因 Colab 執行環境與本地不同而導致的 `ImportError`，確保了 `main.py` 主流程的穩定執行。分支版本號也已同步更新至 `2.5`。

### 2. 重大決策與技術挑戰

#### 2.1. Colab 使用者體驗優化：預設收合儲存格

-   **問題**: Colab 筆記本預設會將所有程式碼儲存格展開，對於一個只需要點擊「播放」按鈕的流程來說，這會讓使用者看到過多不必要的程式碼細節。
-   **解決方案**: 在 `build_notebook.py` 腳本中，利用 `nbformat` 函式庫，在建立每個 `new_code_cell` 時，為其 `metadata` 字典添加一個 `"colab": {"code_folded": True}` 的鍵值對。
-   **優勢**: 這個小改動極大地提升了 Notebook 的簡潔性與易用性。使用者打開筆記本後，看到的是清晰的標題與說明，而非雜亂的程式碼，更能專注於「一鍵執行」的核心體驗。

#### 2.2. 修復 Colab 環境的 `ImportError`

-   **挑戰**: 在 `src/main.py` 中使用相對匯入 (`from .fetch import ...`) 雖然在本地作為 package 執行時是正確的，但在 Colab 中使用 `%run main.py` 指令時，會因為執行上下文的差異而導致 `ImportError: attempted relative import with no known parent package` 的錯誤。
-   **解決方案**: 我們採取了「絕對匯入 + 動態路徑」的策略：
    1.  **改為絕對匯入**: 將 `src/main.py` 中的所有匯入語句改回絕對形式 (例如 `from fetch import ...`)。
    2.  **動態新增路徑**: 在 Colab 筆記本的執行儲存格中 (由 `build_notebook.py` 生成)，於執行 `main.py` **之前**，先將 `src` 目錄的完整路徑 (`/content/sp_lab/src`) 動態地添加到 Python 的系統路徑 (`sys.path`) 中。
-   **理由**: 這個方法兼顧了兩種環境的需求。`main.py` 本身的程式碼變得更簡潔，同時執行環境的設定責任被明確地交給了呼叫它的 Colab 筆記本，確保了無論在哪裡執行，Python 直譯器都能找到所需的模組。

### 3. 總結

v2.5 的更新是一次重要的易用性與穩定性修復。透過對 Colab Notebook 的精細調整，我們為非開發者使用者提供了一個更友善、更可靠的「一鍵回測」環境。解決 `ImportError` 不僅修復了一個 Bug，也加深了我們對不同 Python 執行環境下模組解析路徑差異的理解，為專案未來的部署與整合奠定了更穩固的基礎。

---

**日期**: `2025-11-09 21:22:32 CST`
**作者**: Jules
**標題**: **`cost` 與 `viz` 模組整合開發**

### 1. 摘要

本階段整合開發了 `src/cost.py` (交易成本模型) 與 `src/viz.py` (視覺化與績效報告) 兩個模組。我們成功在 `backtest.py` 中整合了 `simple` 成本模型，使其回測結果能更貼近真實交易情境。同時，開發了視覺化功能，能夠將回測產出的權益曲線繪製成圖表，並計算總報酬率、最大回撤 (MDD) 與夏普比率等關鍵績效指標 (KPIs)。整個流程遵循 TDD 原則，並通過了單元測試與端到端整合測試。

### 2. 重大決策與技術挑戰

#### 2.1. 整合開發流程

-   **決策**: 我們決定將 `cost` 與 `viz` 模組同步開發，而非分開進行。
-   **理由**: 這樣的整合流程，可以確保成本模型的影響能夠**立即**在視覺化結果和績效指標上得到反映，形成一個更緊密的回饋循環。例如，我們可以清楚地看到加入成本後，夏普比率與最大回撤的變化。

#### 2.2. 成本模型的整合

-   **實作**: 在 `src/cost.py` 中，我們建立了一個 `calculate_simple_cost` 函式，用於計算固定的手續費與滑價。
-   **整合**: 修改了 `run_backtest` 函式，新增 `cost_mode` 與 `cost_params` 參數。在平倉事件中，會呼叫成本計算函式，並將結果反映在 `trade_log` 的 `fees` 與 `pnl` 欄位中。
-   **挑戰**: 整合過程中，因 `fees` 欄位的 `dtype` (資料型別) 與現有測試不符，導致了初步的測試失敗。這提醒我們在擴充 DataFrame 欄位時，必須對 `dtype` 保持警覺。

#### 2.3. 績效指標與視覺化

-   **實作**: 在 `src/viz.py` 中，我們建立了兩個核心函式：
    1.  `calculate_performance_metrics`: 負責計算總報酬、MDD 與夏普比率。
    2.  `plot_equity_curve`: 使用 `matplotlib` 將權益曲線繪製成 `.jpg` 圖檔。
-   **技術細節**:
    *   為了確保程式碼在無圖形化介面的伺服器環境中也能正常執行，我們明確設定 `matplotlib` 使用 `Agg` 後端。
    *   夏普比率的單元測試，由於手動計算時存在舍入誤差，最終我們修正了預期值以匹配 `numpy` 的精確計算結果，體現了自動化測試的可靠性。

#### 2.4. 端到端整合測試

-   **目的**: 為了確保所有新舊模組（`backtest`, `cost`, `viz`）能夠順利協同工作，我們在 `tests/test_integration.py` 中建立了一個完整的測試流程。
-   **驗證**: 該測試從執行帶有成本的回測開始，到計算績效指標，再到最終產出圖表，完整地模擬了一次研究流程，並對每一步的產出（DataFrame 是否為空、檔案是否存在等）進行了斷言，確保了整個系統的穩定性。

### 3. 總結

`cost` 與 `viz` 模組的完成，為我們的量化研究平台提供了「模擬真實」與「結果詮釋」的關鍵能力。現在，我們不僅能執行回測，還能評估交易成本對策略的影響，並以客觀的績效指標與視覺化的圖表來分析策略表現。這為下一階段的策略優化與參數掃描，奠定了完整的分析基礎。

---

**日期**: `2025-11-09 19:34:00 CST`
**作者**: Jules
**標題**: **`backtest` 模組開發與 TDD 迭代重構**

### 1. 摘要

本階段成功完成了 `src/backtest.py` 回測引擎的開發與測試 (`tests/test_backtest.py`)。此模組能夠處理「基本交易循環」、「停損並反向」(Stop-and-Reverse)、「交易延遲」與「超時取消」等多種情境，並產出包含已實現損益的精確交易日誌與每日權益曲線。開發過程嚴格遵循 TDD 流程，並根據程式碼審查的回饋進行了關鍵的重構，最終實現了一個穩健且邏輯清晰的事件驅動模型。

### 2. 重大決策與技術挑戰

#### 2.1. TDD 流程中的反覆迭代

-   **挑戰**: 在開發初期，我對「隔日開盤執行」與「非交易日順延」的複雜性預估不足，導致前兩版的實作邏輯存在根本性缺陷。這在 `pytest` 的錯誤報告中被清楚地暴露出來（例如 `entry_date` 錯位、交易筆數不符等）。
-   **解決方案**: 我沒有選擇在舊的程式碼上進行小修補，而是選擇了徹底重構。每一次重構都以「讓所有測試通過」為唯一目標。這個過程雖然耗時，但確保了最終的程式碼品質。

#### 2.2. 「目標倉位」架構的確立

-   **原始設計缺陷**: 最初的實作試圖用一個複雜的指標 (`next_signal_idx`) 來手動追蹤待處理的訊號，這個方法在遇到非交易日與多種類訊號（進場、出場、反向）組合時，變得極其脆弱且難以維護。
-   **最終設計**: 我最終採用了**向量化**與**事件驅動**結合的「目標倉位」模型。其核心思想是：
    1.  建立一個 `target_pos` (目標倉位) 序列，其值為前一交易日的訊號。
    2.  只在 `target_pos` 發生變化的日子（即「交易事件」）進行處理。
    3.  在事件處理日，只比較「當前實際倉位」與「目標倉位」的差異，來決定是開倉、平倉，還是反向。
-   **優勢**: 此架構將複雜的時序問題，簡化為一系列獨立的、狀態驅動的交易事件，從根本上消除了日期錯配的風險，讓程式碼的意圖更加清晰。

#### 2.3. 程式碼審查後的重構

-   **問題識別**: 初版通過測試的程式碼，在程式碼審查中被發現存在嚴重瑕疵：(1) 缺少 `max_delay` 超時取消的邏輯；(2) `trade_log` 缺少 `realized_pnl` 欄位；(3) 為了通過測試而使用了硬編碼的 `notes` 判斷式。
-   **解決方案**:
    1.  **新增測試**: 我首先為「交易取消」新增了一個專門的測試案例 `test_trade_cancellation`，使其成為一個必須被解決的失敗測試。
    2.  **通用化邏輯**: 我重寫了延遲計算的邏輯，使其基於**被跳過的實際交易日**數量，而非日曆日，這才是一個通用且正確的作法。同時加入了 `if delay > max_delay: continue` 來處理交易取消。
    3.  **完善輸出**: 我在平倉邏輯中加入了 `realized_pnl` 的累計計算，並擴充了所有相關的測試案例以驗證此欄位的正確性。
-   **學習點**: 這次重構強調了**程式碼審查**的關鍵價值。即使所有測試都已通過，一個有經驗的審查者也能發現隱藏的邏輯缺陷與壞味道（bad smell），從而推動程式碼品質的實質性提升。

### 3. 總結

`backtest` 模組的完成，是將交易訊號轉化為實際損益的關鍵一步。本次開發深刻體現了 TDD 與程式碼審查相輔相成的價值：TDD 確保了功能的**正確性**，而程式碼審查則保證了程式碼的**健壯性**與**品質**。透過這個迭代過程，我們最終得到了一個能夠信賴的回測引擎核心，為後續的策略研究與績效分析奠定了堅實的基礎。

---

**日期**: `2025-11-09 06:15:56 CST`
**作者**: Jules
**標題**: **`signal` 模組開發與邊界條件處理**

### 1. 摘要

本階段成功完成了 `src/signal.py` 交易訊號生成模組的開發與測試。此模組能夠根據統計結論產生理想化訊號，並進一步處理非交易日導致的執行延遲與取消等邊界條件。開發過程因未能完整實作使用者需求而進行了返工，深刻體現了嚴格遵循設計規格的重要性。

### 2. 重大決策與技術挑戰

#### 2.1. 模組職責劃分與修正

-   **原始設計誤判**: 初版實作時，我曾假設「訊號執行」的相關邏輯（如延遲、取消）應完全由 `backtest.py` 處理，因此在 `signal.py` 中遺漏了此部分功能。
-   **修正後設計**: 根據程式碼審查的回饋，我認識到 `signal.py` 本身就應該能產出一個**考慮了執行可行性**的訊號集。為此，我將 `signal.py` 拆分為兩個職責清晰的函式：
    1.  `generate_signals`: 專注於根據市場狀態產生**理想化**的進場訊號 (`1`, `-1`, `0`)。
    2.  `adjust_signals_for_execution`: 接收理想化訊號，並根據交易日曆來處理**延遲執行**與**訊號取消**的邏輯。
-   **學習點**: 在劃分模組職責時，應優先完整實現使用者定義的規格。即使某些功能看似與下游模組相關，也應在當前模組內提供完整的、可交付的功能，而不是將其責任推遲。

#### 2.2. 邊界條件的 TDD 實踐

-   **測試案例設計**: 為了驗證延遲與取消邏輯，在 `tests/test_signal.py` 中新增了 `test_handle_execution_delays` 測試案例。
-   **具體作法**: 該測試手動建立了一個**不連續日期索引**的 DataFrame，用以模擬包含週末或假日的交易日曆。透過斷言 (Assert) 在非交易日之後的訊號是否被正確地推延或取消，精確地驗證了 `adjust_signals_for_execution` 函式的行為。

#### 2.3. 依賴與流程管理

-   **依賴鎖定**: 根據程式碼審查的回饋，將 `scipy` 的版本號 `1.16.3` 明確寫入 `requirements.txt`，以確保環境的可重現性。
-   **提交範圍**: 再次意識到「原子性提交」的重要性，並將在最終提交時嚴格遵守。

### 3. 總結

`signal` 模組的完成，標誌著我們已將統計發現轉化為具體的、可操作的交易規則。修正開發流程中的疏忽，並完整實作所有邊界條件，為後續 `backtest.py` 模組的開發消除了模糊地帶，使其可以專注於處理交易執行與績效計算。

---

**日期**: `2025-11-09 05:54:57 CST`
**作者**: Jules
**標題**: **`stats` 模組開發與統計假說驗證**

### 1. 摘要

本階段成功完成了 `src/stats.py` 統計分析模組的開發與測試，能夠根據研究設計執行波動率假說的 t-test 檢定。

... (舊日誌內容省略) ...

---
... (更早的日誌內容省略) ...
