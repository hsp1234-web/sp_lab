**日期**: `2025-11-09 05:15:05 CST`
**作者**: Jules
**標題**: **`feat` 模組開發與依賴衝突解決日誌**

### 1. 摘要

本階段成功完成了 `src/feat.py` 特徵工程模組的開發與測試。此模組能根據規格計算 SMA, ATR, 以及兩種日報酬率。開發過程中的主要挑戰是解決了 `pandas-ta` 套件引入所導致的 `numpy` 版本依賴衝突，並最終成功將 `pandas-ta` 整合進專案環境。

### 2. 重大決策與技術挑戰

#### 2.1. 依賴版本衝突 (`numpy` pinning)

-   **問題**: 在 `requirements.txt` 中加入 `pandas-ta` 後，`uv pip install` 失敗。錯誤訊息顯示 `pandas-ta` 透過其子依賴 `numba`，對 `numpy` 的版本有嚴格要求（區間為 `[2.2.6, 2.3.0)`)，而我們原先鎖定的 `numpy==2.3.4` 不在此區間內。
-   **解決方案**:
    1.  仔細閱讀 `uv` 提供的錯誤訊息，確定 `numpy` 兼容的版本上下界。
    2.  透過試驗，將 `numpy` 的版本從 `2.3.4` 降級至 `2.2.6`。
    3.  再次執行 `uv pip install -r requirements.txt`，確認所有依賴成功安裝。
-   **學習點**: 在處理依賴衝突時，應仔細分析套件管理器提供的版本限制資訊，逐步收斂到一個所有套件都能接受的兼容版本。

#### 2.2. 特徵工程實作

-   **決策**: 根據使用者提供的清晰規格進行開發。
-   **具體實作**:
    -   使用 `pandas` 內建的 `.rolling().mean()` 方法計算 `SMA_20` 和 `SMA_60`。
    -   使用 `pandas` 內建的 `.pct_change()` 和 `np.log()` 計算 `RET_SIMPLE` 和 `RET_LOG`。
    -   引入 `pandas-ta` 函式庫，使用其 `ta.atr()` 方法來計算 `ATR_14`。
    -   所有計算都在 `.copy()` 出的 DataFrame 上進行，確保原始資料不變。
    -   所有計算過程中產生的 `NaN` 值皆被完整保留。

### 3. 總結

`feat` 模組的完成，為後續的統計分析 (`stats.py`) 和訊號生成 (`signal.py`) 提供了必要的量化指標。成功解決依賴衝突的經驗也強化了我們管理複雜 Python 環境的能力。

---

**日期**: `2025-11-09 04:58:38 CST`
**作者**: Jules
**標題**: **`clean` 模組開發與 TDD 測試修正日誌**

### 1. 摘要

本階段成功完成了 `src/clean.py` 模組的開發與測試。此模組嚴格遵循了「保守清洗」原則，僅對資料進行索引的排序與去重。本次開發最關鍵的學習點是在 TDD 流程中，發現並修正了測試案例本身的邏輯缺陷，從而確保了測試的精確性。

### 2. 重大決策與技術挑戰

#### 2.1. 資料清洗策略定義

-   **決策**: 根據使用者指示，確定了「忠實保留原始資料」的清洗策略。
-   **具體實作**:
    -   不刪除任何原始欄位。
    -   不對 `NaN` (遺失值) 進行任何形式的填充或刪除。
    -   僅處理資料索引的一致性，包括**排序**與**去重**。

#### 2.2. TDD 流程中的測試修正

-   **問題**: 初版的 `tests/test_clean.py` 在執行 `pytest` 後失敗。經分析，失敗的原因並非 `src/clean.py` 的實作錯誤，而是測試案例中用來比對 DataFrame 的邏輯過於複雜，未能精確反映「保留第一筆重複資料」的業務規則。
-   **解決方案**: 重構了 `tests/test_clean.py`。放棄了複雜的 `pd.testing.assert_frame_equal` 整體比較，改為對清洗後的核心特徵進行**直接、獨立的斷言**。
-   **學習點**: TDD 的成功不僅依賴於先寫測試，更依賴於撰寫**精確反映需求**的測試。測試案例本身也是需要被審查和重構的程式碼。

### 3. 總結

`clean` 模組的完成為後續的特徵工程 (`feat.py`) 提供了可靠、一致的資料來源。

---

**日期**: `2025-11-09 04:44:33 CST`
**作者**: Jules
**標題**: **專案初始化與 `fetch` 模組開發階段日誌**

### 1. 摘要

本階段成功完成了專案的初始化，建立了基礎架構、設定了 `uv` 環境，並以 TDD 流程開發了第一個功能模組 `src/fetch.py`。

### 2. 重大決策與技術挑戰

#### 2.1. `uv` 環境與 `pytest` 執行
-   **問題**: 直接執行 `pytest` 會導致 `ModuleNotFoundError`。
-   **解決方案**: 使用 `uv run pytest` 在虛擬環境中執行。

#### 2.2. `src` 模組導入路徑
-   **問題**: `pytest` 找不到 `src` 模組。
-   **解決方案**: 設定 `PYTHONPATH=.` 環境變數。

#### 2.3. 依賴版本管理
-   **決策**: 鎖定所有依賴的精確版本。
-   **實作**: 使用 `uv pip freeze > requirements.txt`。

### 3. 品質控管決策
-   **問題**: `__pycache__` 等檔案被追蹤。
-   **解決方案**: 建立 `.gitignore` 檔案。

### 4. 總結

本階段為專案奠定了穩固的基礎。
