**日期**: `2025-11-09 04:58:38 CST`
**作者**: Jules
**標題**: **`clean` 模組開發與 TDD 測試修正日誌**

### 1. 摘要

本階段成功完成了 `src/clean.py` 模組的開發與測試。此模組嚴格遵循了「保守清洗」原則，僅對資料進行索引的排序與去重。本次開發最關鍵的學習點是在 TDD 流程中，發現並修正了測試案例本身的邏輯缺陷，從而確保了測試的精確性。

### 2. 重大決策與技術挑戰

#### 2.1. 資料清洗策略定義

-   **決策**: 根據使用者指示，確定了「忠實保留原始資料」的清洗策略。
-   **具體實作**:
    -   不刪除任何原始欄位。
    -   不對 `NaN` (遺失值) 進行任何形式的填充或刪除。
    -   僅處理資料索引的一致性，包括**排序**與**去重**。

#### 2.2. TDD 流程中的測試修正

-   **問題**: 初版的 `tests/test_clean.py` 在執行 `pytest` 後失敗。經分析，失敗的原因並非 `src/clean.py` 的實作錯誤，而是測試案例中用來比對 DataFrame 的邏輯過於複雜，未能精確反映「保留第一筆重複資料」的業務規則。
-   **解決方案**: 重構了 `tests/test_clean.py`。放棄了複雜的 `pd.testing.assert_frame_equal` 整體比較，改為對清洗後的核心特徵進行**直接、獨立的斷言**，包括：
    1.  索引是否唯一。
    2.  索引是否單調遞增。
    3.  `NaN` 值是否被保留在正確的位置。
    4.  **重複索引的資料是否正確地保留了第一筆**。
    5.  最終資料筆數是否正確。
-   **學習點**: TDD 的成功不僅依賴於先寫測試，更依賴於撰寫**精確反映需求**的測試。測試案例本身也是需要被審查和重構的程式碼。

### 3. 總結

`clean` 模組的完成為後續的特徵工程 (`feat.py`) 提供了可靠、一致的資料來源。本次在 TDD 流程中的實踐經驗，也為未來更複雜模組的開發提供了寶貴的品質保證方法。

---

**日期**: `2025-11-09 04:44:33 CST`
**作者**: Jules
**標題**: **專案初始化與 `fetch` 模組開發階段日誌**

### 1. 摘要

本階段成功完成了專案的初始化，建立了基礎架構、設定了 `uv` 環境，並以 TDD 流程開發了第一個功能模組 `src/fetch.py`。過程中解決了 Python 模組導入路徑與 `uv` 虛擬環境執行的核心問題。

### 2. 重大決策與技術挑戰

#### 2.1. `uv` 環境與 `pytest` 執行

-   **問題**: 直接執行 `pytest` 會導致 `ModuleNotFoundError`，因為它使用了系統預設的 Python 環境，而非我們用 `uv` 建立的 `.venv` 虛擬環境。
-   **解決方案**: 使用 `uv run` 指令來確保測試在正確的虛擬環境中執行。最終採用的指令為 `uv run pytest`。

#### 2.2. `src` 模組導入路徑

-   **問題**: 即使使用了 `uv run`，`pytest` 仍然無法找到 `src` 目錄下的模組，導致 `ImportError: No module named 'src'`。
-   **分析**: `pytest` 預設從專案根目錄執行，但 Python 的導入路徑 `sys.path` 未必能正確解析 `src` 為一個頂層套件。
-   **解決方案**: 透過設定 `PYTHONPATH=.` 環境變數，明確地將專案根目錄加入到 Python 的模組搜尋路徑中。最終的完整測試指令為 `PYTHONPATH=. uv run pytest`。

#### 2.3. 依賴版本管理

-   **決策**: 為了確保環境的可重現性，所有依賴套件的版本都必須被鎖定。
-   **實作**: 放棄了在 `requirements.txt` 中只寫套件名稱的作法，改用 `uv pip freeze > requirements.txt` 指令，將當前環境中所有套件的精確版本號寫入檔案。

### 3. 品質控管決策

-   **問題**: 程式碼審查發現 `__pycache__` 等由 Python 自動產生的檔案被納入追蹤。
-   **解決方案**: 在專案根目錄下建立了一個全面的 `.gitignore` 檔案，用以忽略所有不應被版本控制的暫存檔、快取檔與本地環境設定檔（如 `.venv`）。

### 4. 總結

本階段為專案奠定了穩固的基礎。解決的環境設定與路徑問題是未來所有模組開發的基石。TDD 流程的成功導入也確保了程式碼的品質與可測試性。
