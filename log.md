**日期**: `2025-11-09 05:23:21 CST`
**作者**: Jules
**標題**: **[修正] `feat` 模組開發與 `pandas-ta` 設計特性分析日誌**

### 1. 摘要

本階段成功完成了 `src/feat.py` 特徵工程模組。**(修正更新)** 最初認為開發過程中的主要挑戰是 `numpy` 的版本依賴衝突，但經過使用者深入分析後，根本原因被重新定位為 **`pandas-ta` 函式庫的設計特性（僅支援 DataFrame）與使用者預期的落差**。本篇日誌旨在修正先前的結論，並記錄更準確的技術洞見。

### 2. 重大決策與技術挑戰

#### 2.1. 核心問題再定義: `pandas-ta` 的 DataFrame-Only 設計

-   **原始誤判**: 將 `pandas-ta` 無法使用的問題歸咎於 `numpy` 的版本不相容。
-   **根本原因 (修正後)**: `pandas-ta` 的 `.ta` 擴充功能在設計上是為 **Pandas DataFrame** 物件註冊的，並未對 **Pandas Series** 註冊。當嘗試在 Series 上使用 `.ta` 時，會引發 `AttributeError`，此錯誤容易被誤解為環境或依賴問題。
-   **學習點**: 這是典型的「函式庫設計與使用者預期不符」的案例。在面對看似複雜的環境問題時，應將「回頭深入理解函式庫的 API 設計」作為一個優先的除錯方向。

#### 2.2. `numpy` 版本鎖定的背景

-   **決策**: 雖然 `numpy` 的版本衝突並非問題的根本原因，但將其版本鎖定在 `2.2.6` 依然是**必要的操作**。
-   **原因**: 這是為了維持與 `pandas-ta` 的子依賴 `numba` 的兼容性。此決策的背景已被詳細記錄在 `docs/DEPENDENCY_NOTES.md` 中。

#### 2.3. 特徵工程實作

-   **決策**: 根據使用者提供的清晰規格進行開發。
-   **具體實作**:
    -   使用 `pandas` 內建函式計算 `SMA` 和報酬率。
    -   引入 `pandas-ta` 函式庫，並**正確地在其預期的 DataFrame 格式上**使用 `ta.atr()` 方法來計算 `ATR_14`。
    -   所有計算都在 `.copy()` 出的 DataFrame 上進行，並完整保留 `NaN` 值。

### 3. 文件化決策

-   為了將此重要發現固化，已執行以下文件更新：
    1.  在 `src/feat.py` 的 docstring 中增加了 `pandas-ta` 的使用提示。
    2.  建立了 `docs/DEPENDENCY_NOTES.md`，詳細記錄了 `numpy` 和 `pandas-ta` 的注意事項。

### 4. 總結

`feat` 模組的完成為後續分析提供了必要的量化指標。更重要的是，深入分析 `pandas-ta` 問題的過程，為專案的技術知識庫積累了寶貴的經驗。

---

**日期**: `2025-11-09 04:58:38 CST`
**作者**: Jules
**標題**: **`clean` 模組開發與 TDD 測試修正日誌**

### 1. 摘要

本階段成功完成了 `src/clean.py` 模組的開發與測試，嚴格遵循「保守清洗」原則。開發過程中的關鍵學習點是修正了測試案例本身的邏輯缺陷，確保了測試的精確性。

... (舊日誌內容省略) ...

---

**日期**: `2025-11-09 04:44:33 CST`
**作者**: Jules
**標題**: **專案初始化與 `fetch` 模組開發階段日誌**

### 1. 摘要

本階段成功完成了專案的初始化，建立了基礎架構、設定了 `uv` 環境，並以 TDD 流程開發了 `src/fetch.py` 模組。

... (舊日誌內容省略) ...
