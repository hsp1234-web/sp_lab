

# 指數期貨波動率量化研究計畫書

**版本：1.0**
**日期：2025 年 11 月 9 日**
**專案代號：VOL-FUTURE-LAB**

---

## 一、專案願景與核心目標

### 1.1 專案願景

本專案旨在建立一個以 **終端環境（Terminal）可獨立運作**、**使用 uv 管理依賴**、且具備 **高速數據分析與回測能力** 的量化研究平台。
核心重點在於驗證「波動率」與「價格變動」之間的統計關係，並將其轉化為可回測的交易策略，用於台灣與國際主要指數期貨市場。

### 1.2 核心目標

1. **精確數值分析**：
   透過 DuckDB 與 Parquet 格式資料，驗證四種「波動與價格」假說組合，明確量化其平均報酬率與機率分佈。

2. **高速回測引擎**：
   建立一個能於終端環境直接執行的向量化回測流程，支援多策略同時測試。

3. **策略模擬與風控**：
   設計包含交易成本、滑價模擬的模組化回測框架，以模擬實際期貨交易情境。

4. **視覺化與報告輸出**：
   在本階段以靜態圖表與報表（非互動式）為主，產出「權益曲線」、「MDD」、「風報比」等核心分析圖。

5. **測試驅動開發（TDD）導入**：
   導入單元測試與整合測試，確保所有模組能獨立運作且結果可重現。

---

## 二、架構與檔案設計（以 uv 為核心）

### 2.1 環境與依賴管理

* **管理工具**：uv
* **核心依賴檔**：

  * `pyproject.toml`：記錄 Python 版本與主要套件（如 duckdb、pandas、pyarrow、pytest 等）。
  * `uv.lock`：鎖定所有套件版本，確保在任意機器上可重現相同結果。
  * `.python-version`：明確指定 Python 版本，確保一致性。

所有開發、測試與執行皆以 uv 管理環境；不得混用 pip、poetry 或 conda。

---

### 2.2 專案目錄結構

```
project-root/
│
├─ data/                         # 資料儲存區
│  ├─ raw/                       # 原始下載 Parquet 檔案
│  └─ proc/                      # 清洗後與特徵工程結果
│
├─ src/                          # 核心程式模組
│  ├─ cfg.py                     # 環境與參數設定
│  ├─ fetch.py                   # 下載與初步匯入
│  ├─ clean.py                   # 清洗、對齊交易日
│  ├─ feat.py                    # 特徵工程（ATR、MA、報酬率等）
│  ├─ stats.py                   # 統計實驗與假說驗證
│  ├─ signal.py                  # 交易訊號生成
│  ├─ backtest.py                # 回測引擎（含交易邏輯）
│  ├─ cost.py                    # 成本與滑價模擬
│  ├─ viz.py                     # 結果視覺化與報告
│  └─ util.py                    # 共用工具模組
│
├─ tests/                        # 單元與整合測試
│  ├─ test_fetch.py
│  ├─ test_clean.py
│  ├─ test_feat.py
│  ├─ test_stats.py
│  ├─ test_signal.py
│  ├─ test_backtest.py
│  └─ sample_parquets/           # 測試專用小樣本資料
│
├─ docs/                         # 文件與計畫說明
│  ├─ PROJECT_PLAN.md
│  ├─ UV_DEPENDENCY_GUIDE.md
│  ├─ TDD_GUIDE.md
│  └─ ARCHITECTURE.md
│
├─ .python-version
├─ pyproject.toml
├─ uv.lock
├─ README.md
└─ .gitignore
```

---

### 2.3 各模組功能簡述

| 模組          | 功能摘要                       | 輸入               | 輸出               |
| ----------- | -------------------------- | ---------------- | ---------------- |
| cfg.py      | 全域設定、參數常數                  | 無                | 全域參數             |
| fetch.py    | 下載 TAIFEX、yfinance、FRED 資料 | 網路 API           | raw parquet      |
| clean.py    | 清洗與交易日對齊                   | raw parquet      | proc parquet     |
| feat.py     | 特徵工程（ATR、MA、報酬率）           | proc parquet     | features parquet |
| stats.py    | 驗證四大波動假說                   | features parquet | 統計結果 parquet     |
| signal.py   | 產生進出場訊號                    | features parquet | signals parquet  |
| backtest.py | 回測策略                       | signals、價格資料     | trade log、equity |
| cost.py     | 模擬手續費與滑價                   | cfg 參數           | 成本值              |
| viz.py      | 產出靜態圖表與報表                  | equity、trade log | PNG、報告           |
| util.py     | 通用小工具                      | 多模組              | 工具函式             |

---

## 三、開發與執行流程（終端導向）

### 3.1 本地執行原則

* 所有操作皆於 **終端機** 進行，環境以 uv 為核心管理。
* 每個 `.py` 檔可在終端中個別執行，或由 `main.py`（可選）統一調度。
* 每次開發/修改後，應立即執行對應測試檔（TDD 原則）。

---

### 3.2 終端操作流程（文字化概念）

1. **建立環境**

   * 使用 `pyproject.toml` + `uv.lock` 初始化專案環境。
   * 所有依賴由 uv 管理，禁止直接使用 pip。

2. **資料處理**

   * 執行 fetch.py → clean.py → feat.py。
   * 結果皆寫入 `data/proc` 資料夾。

3. **統計與回測**

   * 由 stats.py 驗證假說後，signal.py 生成交易訊號。
   * backtest.py 模擬交易，cost.py 套入手續費與滑價。

4. **視覺化與分析**

   * 使用 viz.py 生成圖表與報表。
   * 報表可放入 docs/ 或直接輸出於終端摘要。

5. **測試與驗證**

   * 所有模組對應 tests 下的檔案，每次更新都要通過 pytest 驗證。

6. **環境重現**

   * 任何人 clone 專案後，只需使用 uv.lock 同步依賴即可完全重現環境。

---

### 3.3 專案輸出

| 階段   | 輸出成果             | 格式             |
| ---- | ---------------- | -------------- |
| 資料階段 | 清洗後日線與特徵資料       | Parquet        |
| 統計階段 | 假說檢驗結果           | CSV / Parquet  |
| 回測階段 | trade log、equity | Parquet        |
| 分析階段 | KPI、報表、圖表        | PNG / Markdown |

---

## 四、測試驅動開發（TDD）原則與策略

### 4.1 開發哲學

> 「先寫測試，再寫功能。」
> 所有開發均以測試為導向，每個功能模組在編寫前必須先定義測試案例與預期行為。

### 4.2 測試架構

* **測試工具**：pytest（由 uv 管理）。
* **測試資料**：`tests/sample_parquets` 內放入極小型樣本（20 筆日線資料），作為統一測試基準。
* **測試層級**：

  1. 單元測試（unit test）：每個模組功能獨立驗證。
  2. 整合測試（integration test）：模組間資料流動是否正確。
  3. 端到端測試（E2E）：從 fetch → clean → feat → stats → backtest 全流程小樣本測試。

---

### 4.3 測試設計原則

| 類型               | 測試重點      | 驗證項目                     |
| ---------------- | --------- | ------------------------ |
| test_fetch.py    | 資料下載與儲存   | parquet 是否正確建立、schema 符合 |
| test_clean.py    | 缺值與交易日對齊  | 缺值標記、日期順序                |
| test_feat.py     | ATR、MA 計算 | 數值與參數一致性                 |
| test_stats.py    | 假說驗證      | 分組樣本數、平均報酬、顯著性           |
| test_signal.py   | 訊號生成      | 條件邏輯與重複處理                |
| test_backtest.py | 回測        | 策略損益、手續費影響、equity 長度     |
| test_cost.py     | 成本模擬      | 滑價/手續費模型輸出合理             |
| test_viz.py      | 視覺化       | 圖檔是否生成、欄位是否完整            |

---

### 4.4 驗證流程（TDD 週期）

1. **定義測試需求**：在 tests 中撰寫測試描述與預期行為。
2. **最小實作**：在 src 模組撰寫最小可通過測試的功能。
3. **執行測試**：於終端執行 pytest 驗證所有案例。
4. **重構與優化**：功能通過測試後，進行效能或結構優化。
5. **持續整合**：所有修改都需通過完整測試後才能合併主分支。

---

### 4.5 成功標準

* 所有測試案例（unit + integration + E2E）通過率需達 **100%**。
* 任意開發者在不同主機上使用 uv.lock 還原環境後，執行測試結果必須一致。
* 任意模組的修改不得破壞上游或下游資料結構。

---

## 附錄：開發理念摘要

* **以 uv 為中心的依賴管理**：確保環境一致與快速部署。
* **Parquet + DuckDB 架構**：高速處理大規模資料，無需額外資料庫伺服器。
* **終端可重現性**：任何人只要有終端與 uv，即可完整重現專案流程。
* **TDD 作為品質保證核心**：先測試、後開發、再重構。

---

📘 **本計畫書為最終版本 1.0，所有後續修改請以此為主。**
後續若需擴展 Colab、視覺化互動或自動化部署，將另立附錄文件。

---

你可以直接把以上內容貼進任何 Markdown 編輯器（例如 VS Code、Obsidian、Typora）並保存成：
**`指數期貨波動率量化研究計畫書.md`**

是否希望我幫你補上一個「簡短的執行測試與初始化流程說明區（純文字版，不含指令）」？
那會放在附錄前，方便未來交給工程師或 AI。
